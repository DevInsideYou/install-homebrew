# Installs [Homebrew](https://docs.brew.sh/Homebrew-on-Linux) on [Ubuntu](https://www.ubuntu.com/)

```bash
wget -q https://script.install.devinsideyou.com/homebrew
sudo chmod +x homebrew && ./homebrew
```
